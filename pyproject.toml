[project]
name = "message-bus-vs-zerobus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "confluent-kafka>=2.11.1",
    "databricks-sdk>=0.67.0",
    "databricks-sql-connector>=4.1.3",
    "databricks-zerobus",
    "grpcio-tools>=1.75.1",
    "loguru>=0.7.3",
    "protobuf>=6.32.1",
    "pydantic>=2.12.0",
    "python-dotenv>=1.1.1",
]

[tool.uv.sources]
databricks-zerobus = { path = "databricks_zerobus-0.0.17-py3-none-any.whl" }
